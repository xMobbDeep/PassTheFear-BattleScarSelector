# Private build inputs

Put assets extracted from your own copy of *Pass The Fear* here before running the build script. The directory is ignored by Git because the contents are game-owned or generated from the game.
